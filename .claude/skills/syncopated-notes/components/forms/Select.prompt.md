Native dropdown styled to match `Input`, with a custom chevron.

```jsx
<Select options={["Field", "Tenor", "Mode"]} onChange={onPick} />
<Select options={[{value:"md",label:"Markdown"},{value:"txt",label:"Plain"}]} />
```
