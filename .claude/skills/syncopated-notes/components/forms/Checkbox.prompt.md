Square checkbox with a coral fill when set; supports an `indeterminate` mixed state.

```jsx
<Checkbox checked={a} onChange={setA} label="Enforce policy" />
<Checkbox indeterminate onChange={selectAll} label="Select all" />
```
