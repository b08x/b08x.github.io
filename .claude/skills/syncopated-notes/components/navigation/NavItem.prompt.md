Sidebar / table-of-contents link with a coral left-rule and tinted active surface, matching the knowledgebase navigation.

```jsx
<NavItem active>The Natural Pacing Protocol</NavItem>
<NavItem depth={1} href="#sfl-metadata">SFL Metadata</NavItem>
```

Use `depth` to indent nested TOC entries; `active` for the current page/section.
