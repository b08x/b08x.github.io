Inset terminal-flavored command/output panel (the notes "code-panel" pattern); use for prompts, shell snippets, and example output.

```jsx
<CodePanel lang="bash" filename="~/notes">
sync pull --tag agentic   # 14 observations
</CodePanel>
```

Optional `lang` chip, `filename`, and a built-in `copy` button.
