---
id: notebook
aliases:
  - Welcome!
tags: 
layout: graph
permalink: /notebook
title: Notebook
---


<sitetitle># syncopated notes</sitetitle>

<!-- <strong>Recently updated notes</strong>
<ul>
  {% assign recent_items = site.notes | sort: "date" | reverse %}
  {% for item in recent_items limit: 25 %}
  {% if item.draft != 'true' %}
    <li>
      {{ item.last_modified_at | date: "%Y-%m-%d" }} — <a class="internal-link" href="{{ site.baseurl }}{{ item.url }}">{{ item.title }}</a>
    </li>
  {% endif %}
  {% endfor %}
</ul> -->

