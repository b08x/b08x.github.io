---
---

![[Pasted image 20240202164532.png]]


![[Pasted image 20240202170106.png]]

![[Pasted image 20240202170721.png]]


![[Pasted image 20240202170807.png]]

![[Pasted image 20240202170909.png]]


![[Pasted image 20240202171143.png]]

![[Pasted image 20240202171259.png]]


![[Pasted image 20240202171609.png]]
> [!ai]+ AI
>
> The image shows a Python code that defines two functions: `Find_Center` and `Mark_Allocation`.
> The `Find_Center` function takes a region definition (r) as input, calculates the distance from each point in the region to the nearest background point, finds the maximum distance value, and returns the corresponding background point as the center of the region.
> The `Mark_Allocation` function is used for marking allocations on a map. It takes a list of regions (R) as input, sorts them based on their areas in ascending order, excludes k-1 regions from consideration when selecting a new allocation point, and then uses the `Find_Center` function to find the center of each region. The centers are returned as an output.
